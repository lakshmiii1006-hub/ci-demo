{
  "name": "ci-demo",
  "version": "1.0.0",
  "scripts": {
    "build": "echo \"build step (demo) - replace with real build command\"",
    "test": "jest",
    "lint": "eslint . --ext .js"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "eslint": "^8.0.0"
  }
}
